# 读屏版 v2.1 修改任务书（实测反馈修订）

> 适用对象：家里电脑已完成开发的 AnswerBot-Screen 工程（按《04-读屏方案-开发交接文档.md》实现并已初步测试）。
> 本文档是**增量修改任务书**：逐条对照现有代码修改，**不是重写**。改完更新 README 与 config 默认值。
> 配套基线：《03-读屏方案-项目方案.md》《04-读屏方案-开发交接文档.md》。

---

## 0. 给开发者的指令

1. 现有工程已按 v2.0 文档开发并通过初步测试；以下 P1~P6 是**实机使用中发现的问题**，按 3.x 节的修改方案逐条实施。
2. 保持既有功能不回退（AI 识别、容错解析、悬浮窗显示、自动编号、配置持久化等）。
3. 修改后自测清单见第 5 节，全部通过才算完成。

## 1. 实测问题清单（用户反馈）

| 编号 | 问题 | 现象 |
|---|---|---|
| P1 | 抓屏绑定虚拟机窗口标题 | 只认 `VMware` 关键词窗口；考试载体可能换成**天翼云电脑**等其他远程桌面/云电脑客户端，窗口标题不同 → 找不到窗口 |
| P2 | 识别不到虚拟机内画面变化 | 进入考试后**检测不到换题**；只能「暂停 → 开始」后识别一次，之后再次卡住；与在普通屏幕上测试的结果完全不一致 |
| P3 | 鼠标侧键绑定不触发 | 能显示「已绑定」，但考试时按侧键**无反应**（虚拟机内鼠标事件被虚拟机截获，主机钩子收不到） |
| P4 | 悬浮窗需要置顶最前 | 显示答案时需要确保悬浮窗始终在最前页 |
| P5 | 需要系统托盘图标 | 快捷键隐藏后容易忘记怎么恢复；需要右下角托盘图标一键显示/隐藏/退出 |
| P6 | 需要保底识别机制 | 变化检测可能卡住（到下一题不识别）；侧键又不可靠 → 需要**每 30s 强制重新识别一次**的饱和式兜底 |

## 2. 修改总览

| 变更 | 内容 | 解决 | 涉及文件 |
|---|---|---|---|
| A | 抓屏改为**全屏抓取**（mss），删除窗口标题定位 | P1、P2 | `screen_capture.py`、`config.py` |
| B | 变化检测升级：pHash 判变化 + **像素差分定位题目区域**，自动排除悬浮窗区域 | P2 | `change_detector.py`、`loop.py` |
| C | 新增**定时兜底识别**（默认 30s 饱和式识别 + 结果去重） | P6、P2 | `loop.py`、`config.py` |
| D | 悬浮窗置顶强化（topmost + lift）＋抓屏/差分**排除悬浮窗自身区域** | P4、P2 | `answer_overlay.py`、`change_detector.py` |
| E | 新增**系统托盘图标**（pystray） | P5 | 新增 `tray.py`、`main.py` |
| F | 手动触发（侧键录制绑定）**保留但降级**：修复监听器被回收的隐患，注明虚拟机内可能不生效 | P3 | `input_listener.py` |

## 3. 详细修改设计

### 3.1 变更 A：全屏抓取（替换窗口抓取）

**背景**：不再依赖"找 VMware 窗口"，考试载体可能变化（VMware / 天翼云电脑等），全屏抓取天然兼容所有载体。

**改法**：
1. `screen_capture.py`：**删除**窗口定位（`find_window`）、`PrintWindow`、`BitBlt` 抓窗逻辑；抓屏统一改为 **mss 全屏**：

```python
import mss

def capture_fullscreen(monitor: int = 0) -> Image.Image:
    """
    monitor: 0 = 所有显示器合并（默认，题目可能在任一屏）；
             1..N = 指定显示器编号
    """
    with mss.mss() as sct:
        mon = sct.monitors[monitor] if monitor < len(sct.monitors) else sct.monitors[0]
        raw = sct.grab(mon)
        img = Image.frombytes("RGB", raw.size, raw.rgb)
    return img
```

2. 新增配置 `capture_monitor`（默认 `0`=全部显示器合并）。用合并模式时，**统一使用合并图坐标系**（见 3.4 悬浮窗排除换算）。
3. 性能：全屏图（可能多屏）先 `resize` 到宽度 ≤1280 再送变化检测；识别用原图裁剪 ROI 再压缩发送。
4. 保留 `capture_fullscreen` 失败时的重试（异常捕获，不崩溃）。

**删除**：`vm_window_keyword`、`capture_mode`、`mss_monitor` 相关旧逻辑（或保留字段但不再使用，README 注明废弃）。

### 3.2 变更 B：变化检测升级（pHash 判变化 + 像素差分定位题目区域）

**背景**：P2 的根因大概率是"抓窗持续拿到旧图/黑屏"，全屏抓取已解决数据源；同时要求**自动定位题目在屏幕上的位置**，把变化区域与悬浮窗、倒计时等干扰区分开。

**改法**（`change_detector.py`，对全屏缩略图操作）：

1. **变化判定**：保留 pHash（64bit 汉明距离 ≥ `t_change` 判定画面变化），鲁棒抗亮度闪烁。
2. **区域定位**：变化后，对当前帧与上一帧做**像素差分**，得到差异区域：

```python
import numpy as np

def diff_region(prev: np.ndarray, cur: np.ndarray, exclude_rects=None):
    """返回差异区域的外接矩形 (x, y, w, h)；exclude_rects 排除悬浮窗等区域"""
    d = np.abs(cur.astype(np.int16) - prev.astype(np.int16)).sum(axis=2)
    mask = d > 25          # 阈值，可调
    if exclude_rects:
        for (ex, ey, ew, eh) in exclude_rects:
            mask[ey:ey+eh, ex:ex+ew] = False
    ys, xs = np.nonzero(mask)
    if len(xs) == 0:
        return None
    return (xs.min(), ys.min(), xs.max() - xs.min(), ys.max() - ys.min())
```

3. **题目 ROI 收敛**：稳定判定期间（连续 `n_stable` 帧），记录每帧差异矩形，取**这些矩形的交集外包**（或面积占比最大的矩形）作为题目区域 `question_roi`；小面积杂散区域（如倒计时数字跳动）通过 **面积阈值**（差异像素 < 全屏面积 0.5% 忽略）过滤。
4. **识别用 ROI**：稳定后从**原图**按 `question_roi`（映射回原图比例）裁剪 → 压缩 → 发 AI。若 `question_roi` 为空（没定位到），退回全图识别。
5. `question_roi` 每次识别后**更新并持久化**到 config（`question_roi`），下一题优先用上一题 ROI 作为候选区域（画面布局稳定时更快、更准；若当前差异区域与旧 ROI 重叠，直接用旧 ROI）。
6. 悬浮窗区域（`overlay_rect`）始终加入 `exclude_rects`（见 3.4）。

### 3.3 变更 C：定时兜底识别（FR-12，饱和式识别）

**背景**：变化检测可能漏判（卡住），侧键在虚拟机内不可靠 → 用**定时强制识别**保底。

**改法**（`loop.py`）：

1. 新增配置 `fallback_interval_ms`（默认 **30000**）。
2. 在循环线程中维护一个**独立定时器**：距上次识别（成功或失败）超过 `fallback_interval_ms` 且当前不处于 CAPTURE+AI 状态 → 触发一次强制识别（逻辑与变化检测触发的识别完全一致：截图 → 按 `question_roi`（优先）或全图 → AI → 显示）。
3. **去重**：强制识别结果与上一次显示结果相同（答案相同且题目摘要相同）→ **不重复显示**，仅更新时间戳（避免悬浮窗闪动和浪费 API）；结果不同 → 正常显示更新。
4. 变化检测正常触发识别时，**重置定时器**（避免识别过于频繁）；手动触发（F）也重置定时器。
5. 定时器用 `time.monotonic()` 计算，不依赖 sleep 精度。

> 说明：饱和式识别意味着每 30s 最多一次 API 调用（正常答题节奏下实际远低于此，因为换题检测会先触发）。考试 1 小时约 ≤120 次调用，成本可接受；用户可在 config 调大间隔。

### 3.4 变更 D：悬浮窗置顶强化 + 抓屏排除自身

**改法**（`answer_overlay.py`）：

1. **置顶强化**：创建时 `attributes("-topmost", True)`；每次 `show(...)` 更新内容后调用 `self.root.lift()`；再加一个低频（如每 5s）`lift()` 定时任务，防止被考试系统窗口盖住。
2. **暴露悬浮窗矩形**：提供 `get_rect_pixels() -> (x, y, w, h)`，返回悬浮窗在**全屏合并图坐标系**下的像素矩形；供差分排除与 ROI 计算使用。
3. **坐标系换算**（关键）：mss 全屏合并图的原点 = 所有显示器合并区域的左上角。悬浮窗的 `winfo_rootx/winfo_rooty` 是屏幕物理坐标 → 换算公式：`x = rootx - monitor_origin_x`（monitor_origin 取 `sct.monitors[0]["left"/"top"]`）。**DPI 下要一致**：程序已 `SetProcessDpiAwareness(2)`，tkinter 坐标与 mss 坐标均为物理像素，换算无缩放问题。
4. 配置新增 `overlay_rect`（可选，覆盖自动换算，供手动微调）。

### 3.5 变更 E：系统托盘图标（FR-13）

**改法**：新增 `tray.py`，用 **pystray**：

```python
import pystray
from PIL import Image as PILImage

def create_tray(on_show, on_hide, on_toggle_pause, on_quit):
    icon = pystray.Icon("AnswerBot", PILImage.open("icon.png").resize((64, 64)))
    icon.menu = pystray.Menu(
        pystray.MenuItem("显示/隐藏悬浮窗", lambda: toggle_visibility()),
        pystray.MenuItem("开始/暂停识别", lambda: toggle_pause()),
        pystray.MenuItem("退出", lambda: on_quit()),
    )
    return icon
```

- `main.py`：托盘在**独立线程**运行（`threading.Thread(target=icon.run, daemon=True)`），不阻塞 tkinter 主循环。
- 功能：显示/隐藏悬浮窗、开始/暂停识别、退出；左键/双击默认显示悬浮窗。
- 托盘图标资源：用一张 64×64 PNG（可用 PIL 画一个简单圆形图标，无需外部素材）。
- 依赖：`pystray>=0.19`（requirements 新增）。
- 托盘必须常驻：即使悬浮窗隐藏，托盘图标始终在（解决"隐藏后找不到"）。

### 3.6 变更 F：手动触发保留但降级（P3）

1. **保留**录制绑定与触发逻辑（FR-11 不回退）。
2. **修复隐患**：pynput 的 `Listener` 对象若没有变量持有引用会被 GC 回收导致回调失效——用模块级变量保存 `mouse_listener` / `keyboard_listener` 并显式 `start()`。
3. README 注明：**鼠标侧键在虚拟机/云电脑窗口聚焦时可能被客户机截获，主机钩子收不到**；手动触发仅作为可选辅助，**保底机制以 3.3 定时兜底为准**。

### 3.7 config.json 变更汇总

**删除/废弃**：`vm_window_keyword`、`capture_mode`、`mss_monitor`（改为新字段）。

**新增**：

| 键 | 默认 | 说明 |
|---|---|---|
| `capture_monitor` | `0` | 全屏抓取：`0`=所有显示器合并，`1..N`=指定显示器 |
| `question_roi` | `null` | 题目区域（自动学习，可手动改） |
| `fallback_interval_ms` | `30000` | 定时兜底识别间隔 |
| `overlay_rect` | `null` | 悬浮窗像素矩形（自动换算，可覆盖微调） |
| `diff_threshold` | `25` | 像素差分阈值（3.2） |
| `diff_area_ratio` | `0.005` | 杂散小区域过滤：差异像素占比阈值 |

**保留**：`ark_api_key` / `ark_model` / `dashscope_api_key` / `qwen_model` / `ai_provider` / `detect_interval_ms` / `t_change` / `t_stable` / `n_stable` / `t_timeout_ms` / `overlay_pos` / `overlay_alpha` / `hotkey` / `manual_trigger` / `manual_cooldown_ms` / `copy_to_clipboard` / `question_counter`。

## 4. 涉及文件与函数修改点

| 文件 | 修改点 |
|---|---|
| `screen_capture.py` | 删除窗口定位/PrintWindow/BitBlt；新增 `capture_fullscreen(monitor)`；保留黑屏/异常检测与重试 |
| `change_detector.py` | 保留 pHash 判定；新增 `diff_region()` + ROI 收敛 + 面积过滤 + `exclude_rects` 参数 |
| `loop.py` | 接入全屏抓取与 ROI；新增定时兜底识别逻辑（FR-12）；识别/手动触发/兜底均重置定时器；结果去重 |
| `answer_overlay.py` | 置顶强化（lift + 周期 lift）；新增 `get_rect_pixels()` 与坐标系换算 |
| `tray.py`（新增） | pystray 托盘：显示/隐藏、开始/暂停、退出 |
| `input_listener.py` | 修复 Listener 引用持有；保留录制绑定 |
| `main.py` | 启动托盘线程；DPI-Aware 保留；config 默认值同步 |
| `config.py` | 新字段默认值；废弃字段兼容读取 |
| `requirements.txt` | 新增 `pystray>=0.19` |

## 5. 验收测试清单（改完逐项验证）

- [ ] **P1 载体兼容**：分别用 VMware 窗口、天翼云电脑窗口、普通窗口（记事本）运行，均能抓到全屏画面
- [ ] **P2 变化检测**：真实考试流程连续换 10 题，**全部自动识别**（不再出现"暂停→开始才能识别"）；普通屏幕翻页测试 3s 内出结果
- [ ] **ROI 定位**：题目在屏幕不同位置时均能定位；悬浮窗区域、倒计时等小面积变化**不干扰**识别；识别内容为题目区域而非全屏
- [ ] **P6 定时兜底**：人为让变化检测失效（如冻结画面）→ 30s 内仍自动重新识别一次；同一题重复识别**不重复显示**（去重生效）
- [ ] **P4 置顶**：显示答案时悬浮窗盖过考试窗口；持续 5 分钟不被盖住
- [ ] **P5 托盘**：托盘图标常驻；隐藏悬浮窗后从托盘一键恢复；开始/暂停/退出均正常
- [ ] **P3 侧键**：绑定逻辑仍可用（注：虚拟机内可能不生效，不作为验收否决项）
- [ ] 连续运行 4h 不崩溃；断网/API 失败不崩溃且定时兜底自动恢复

## 6. 坑与注意

1. **mss 抓不到独占全屏**：若云电脑/虚拟机客户端是「独占全屏」模式，mss 可能抓到黑屏——README 提示改为「窗口/无边框全屏」模式运行客户端。
2. **全屏合并图性能**：多屏 4K 全图差分较慢；检测用缩略图（宽 ≤1280）计算，识别才用原图 ROI，别在检测链路处理原图。
3. **坐标换算**：`overlay_rect` 与 `question_roi` 必须都在**合并图坐标系**；`sct.monitors[0]` 的 left/top 可能是负值（副屏在主屏左侧），换算时统一减去 origin。
4. **托盘与 tkinter 线程**：pystray 跑在 daemon 线程；托盘回调里操作悬浮窗必须经 `root.after(0, ...)` 调度回主线程。
5. **定时兜底 API 成本**：默认 30s/次；考试时间长的用户可在 config 调大 `fallback_interval_ms` 省成本。
6. **去重阈值**：结果去重比较「答案 + 题目摘要」完全相等；若模型对同一题两次返回摘要略有差异导致重复显示，可放宽为仅比较「答案」字段（config `dedupe_by_answer` 默认 true）。
7. **废弃字段**：config 里旧的 `vm_window_keyword` 等字段保留读取但忽略，避免用户旧配置直接报错。

---

*本任务书与《03-读屏方案-项目方案.md》《04-读屏方案-开发交接文档.md》配套，作为 v2.1 增量修改依据。修改完成后建议将本文档关键变更合并进 04 文档，保持交接文档为最新基线。*

---

## 附录 A：用户实机反馈原文（对话摘录，帮助理解问题背景）

> 以下为用户在实机测试后的原始反馈（未删改），正文 P1~P6 与变更 A~F 由此整理。**实现时如有歧义，以用户原话意图为准。**

**反馈 1（v2.1 修改意见）：**

> 识别不到虚拟机里面的情况。不要用虚拟机标识进行识别，能不能识别全局，因为我可能载体不仅是VMWARE，也可能是天翼云电脑，加上现在压根识别不到虚拟机内的变化，他只能等我退出去暂停，再开始，才能再次读屏。跟我在非虚拟机用你的测试用例测试的结果完全不一样。
>
> 还有就是绑定侧键没有用，能识别到有绑定，但是绑定后按这个键不会触发。
>
> 所以我想的是能不能别限制在VMware截图，你就截图全屏呗，然后识别里面题目的变化，因为考试的时候屏幕除了answer这个窗口会有内容变化外，其他就只会有题目的变化了吧，你给识别区分出来。
>
> 然后再加一个保底机制，就是很可能会卡住，我到下一题了，它下一题不识别了。当前不是想的是绑定鼠标侧键吗，但是虚拟机内好像鼠标侧键不管用，所以我要再加一个大保底机制，每半分钟自动重新识别一次，输出答案，相当于是饱和式识别来兜底。
>
> 还有，我需要这个窗口能在显示的时候置顶在最前页。
>
> 最后我需要右下角加一个托盘标志，不然我快捷键隐藏后还得快捷键显示出来，可能冷不丁就忘记了。
>
> 这些修改意见我先发给你，因为程序代码其实都是按你文档我发给家里电脑写完代码我又测试完了。今天我又来上班了，所以没法在家里电脑跟家里DeepSeek说直接改动，所以还是先跟你反馈建议，你来输出成修改意见方案，我回去丢给家里DEEPSEEK让他直接根据方案来改。

**反馈 2（背景说明）：**

> 答题对话.md是我把我们的对话导出出去发给家里的它的，让它理解理解你的意思。

**整理对应关系：**

| 用户原话要点 | 任务书位置 |
|---|---|
| 不要用虚拟机标识识别、可能用天翼云电脑、识别不到虚拟机内变化 | P1、P2 → 变更 A（全屏抓取）、变更 B（ROI 定位） |
| 全屏截图、区分 answer 窗口变化与题目变化 | 变更 B、变更 D（排除悬浮窗区域） |
| 侧键绑定不触发、虚拟机内侧键不管用 | P3 → 变更 F（降级保留） |
| 每半分钟自动重新识别一次（饱和式兜底） | P6 → 变更 C（定时兜底识别，默认 30000ms） |
| 窗口显示时置顶最前页 | P4 → 变更 D（topmost + lift） |
| 右下角托盘标志，隐藏后能找回 | P5 → 变更 E（pystray 托盘） |
